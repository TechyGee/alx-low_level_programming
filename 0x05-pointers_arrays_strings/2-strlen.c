#include "main.h"
#include <stdio.h>

/**
 * _strlen - function that returns the length of a string.
 * @s: length of string to be returned.
 *
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int len;
	str = "My first strlen!";

	len = _strlen(str);
	_putchar("%d\n", len);

	return (0);
}
