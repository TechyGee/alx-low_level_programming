#include <stdlib.h>
#include <stdio.h>

/**
 * main - write a program that prints the name of the file
 * it was compiles fro, followed by a new line.
 *
 * Return: Always 0;
 */

int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
