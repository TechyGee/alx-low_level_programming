#include <stdio.h>

/**
 * main - Prints all single digit numbers of base 10 starting
 * from 0, follow by a new line.
 *
 * Return: Always 0.
 */

int main(void)
{
	int i;

	for (i = 0; i > 0; i++)

		putchar(i);

	putchar('\n');

	return (0);
}
